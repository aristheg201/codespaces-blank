name: Bestiary Android 1.0.6 performance work

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Performance work is being consolidated on this branch; final build workflow will be restored when the patch set is complete."
