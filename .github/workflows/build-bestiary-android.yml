name: Bestiary Android 1.0.6 resource-pack stability work

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Resource-pack stability build completed; CI held for device validation."
